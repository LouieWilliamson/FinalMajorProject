using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerSpawnRoom : MonoBehaviour
{
    //public SpawnTile[] DecorSpawns;
    //public int NumOfDecorations;
    //private int decorCount;
    //private int randomSpawn;

    // Start is called before the first frame update
    void Start()
    {
    //    decorCount = 100;

    //    while (decorCount <= NumOfDecorations)
    //    {
    //        randomSpawn = Random.Range(0, DecorSpawns.Length);

    //        DecorSpawns[randomSpawn].enabled = true;
    //        decorCount++;
    //    }
    }
}
